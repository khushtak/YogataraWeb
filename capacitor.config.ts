import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yogatara.learning',
  appName: 'Yogatara Learning',
  webDir: 'dist'
};

export default config;
